- Chat and project list now update live over a WebSocket, so it almost never needs a manual refresh
- Files update live too: the shared folder, archive contents, and file previews refresh on their own
- New Claude service status in the Monitor, with component health and active incidents from status.claude.com
- Redesigned tab switcher on mobile, plus landscape and edge-to-edge layout fixes
- Background task notifications appear as their own block instead of raw text
- A more reliable message queue and assorted mobile UI fixes

> [!NOTE]
> The web version is available at https://cconnect.pages.dev/